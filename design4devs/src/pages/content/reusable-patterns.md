#### Same button styles, inputs, spacing everywhere. If a pattern exists, use it consistently.

1. Create reusable components
2. Same action = same visual treatment
3. Build a design system
4. Users learn once, apply everywhere
